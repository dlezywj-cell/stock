股票估值系统

自选组合
1. 在股票表格中勾选股票，点击「保存选中」，输入组合名称。
2. 下次在顶部「⭐ 自选」下拉框选择组合，即可查看全部成员。
3. 同一只股票可以加入多个组合；原有属性/赛道保持独立。
4. 选择组合后点击「管理」，可改名、追加选中、移除选中或删除组合。
   追加其他股票：先选「全部股票」并勾选，再切回目标组合，点击「管理 → 追加选中」。
   勾选会跨筛选保留；可点「清空」重新选择。
5. 组合自动保存在本机。点击「保存到 GitHub」后，其他设备拉取数据即可使用。
   页面刷新时会保留未上传的本地修改；手动拉取并确认覆盖会使用云端版本。
6. 切换组合会清除代码、属性、市场、列筛选和「仅选中」条件；保留排序和历史截面。
7. 删除组合或移除成员不会删除股票。移除最后一个成员时，空组合会自动消失。

数据兼容
data.json 仍然是股票数组；组合成员记录在每只股票可选的 watchlists 字符串数组中。
旧数据无需迁移，组合随现有 GitHub 保存/拉取流程同步。

Score
涨跌后为 Score 列，点击表头切换高低排序，点击得分查看明细。缺失评分排在最后。
总分 = 估值分×置信系数×60% + 趋势分×40%，保留一位小数。
每种估值方法分别计算：估值分 = 上行分×60% + 赔率分×40%。
上行分：上涨空间100%为100分，负空间计0，最高100分。
赔率分：赔率 = 上涨空间 / 下跌空间绝对值，4倍为100分，最高100分。
无上行空间时该方法计0分；下限目标不低于现价且有上行空间时，赔率分计100。
PE、PS均有效时取平均，仅一种有效时估值分打八折。
市值、预测或估值倍数必须是有效正数，估值下限不得超过上限。
趋势：MA5 > MA10 > MA20为100分，反向为0分，其他有效排列为50分。
无有效估值、市值或趋势数据不足时，总分显示「—」。
置信系数按自然月：≤2个月1，＞2至4个月0.7，＞4至6个月0.5，＞6个月0；日期缺失或异常取0。
置信系数仅调整估值分，趋势分不随预测日期衰减。
预测日期优先使用 forecastUpdatedAt；旧记录从净利、营收、首次创建或确认预测日志中读取。
修改净利或营收预测、新增股票或勾选「确认预测」后保存，会更新预测日期。
仅修改估值倍数、名称、标签、普通备注或自选组合不会刷新预测日期。
原有日期列仍显示最新操作日志日期；评分使用的预测日期可在得分明细中查看。
历史截面采用截面价格/趋势与现有预测，置信系数按今天计算，不是历史预测回测。
Score 动态计算，不写入 data.json；forecastUpdatedAt 随股票数据同步。

历史区间涨跌幅
查看历史截面时，在 Score 后显示「区间涨跌幅」，点击表头可排序；回到最新时自动隐藏。
计算方式：(最新可用价格 / 所选日期收盘价 - 1) × 100%，保留两位小数。
所选日期非交易日时，使用此前最近交易日的收盘价；仅比较价格，不含现金分红。
查询历史截面会刷新最新行情，暂停实时刷新时也会更新区间终点价格。
缺少有效起点或最新价格时显示「—」，排序时放在最后；计算不依赖市值或估值是否完整。
原「涨跌」列仍为截面日单日涨跌幅。

浏览器回归检查（使用模拟数据和 API，不访问真实股票数据）
安装 Chrome 和 playwright 后运行：node tests/watchlists.cjs
可通过 PLAYWRIGHT_MODULE 指定已有 playwright 的模块路径。
纯评分公式检查：node --test tests/score.cjs

Score 策略回测
首页顶部点击「策略回测」，进入 backtest.html；与股票池共用 GitHub 设置。
默认初始资金1,000万元人民币、Score最高的前20只、每周调仓，可改日期、数量、市场及日/周/月周期。
先选日期再「开始回测」。首次需读取历史股票池和逐股行情，可能需要几分钟；可取消，缓存可在设置中清除。
显示累计/年化收益、最大回撤、波动率、夏普、成本、净值与回撤曲线、每次调仓的入选名单与数据覆盖。
可测试成本翻倍，导出净值CSV、成交CSV，或导出数据JSON用于同数据重跑/导入。结果与数据不自动上传。

回测数据与限制
1. 独立回测页按 GitHub data.json 的实际提交记录恢复预测及股票池成员，不使用今天的预测覆盖过去。
   默认数据仓库保存历史始于2025年12月19日；开始日期须在首份历史保存记录之后。
   每个交易日UTC 00:00为预测截止时点，只用此前保存的版本和此前行情；置信系数按该调仓日计算。
   首页原有历史截面查询仍是当前预测配历史行情，与回测页的历史预测口径不同。
2. 行情由股票池现有代理获取 Yahoo Finance 日线；复权收盘和复权开盘近似计入分红再投资。
   趋势使用历史原始收盘价。默认历史市值=最新市值×历史原始收盘价/最新价格，即固定最新股本估算。
   历史股本、拆股、增发、回购可能造成估值和排名偏差；这不是完全消除前视偏差的严格回测。
   行情/市值缺失会排除标的并列明；被删除的历史股票有数据即可参与，但退市与数据不可得仍可能引入存活偏差。
3. 从开始日期起首次建仓；日/周/月按交易日、周一所在周、自然月分组，在首个可用交易日调仓。
   各市场按亚洲、美股开盘先后执行，用上一个可用收盘信号，禁止后来市场的卖出资金提前用于较早市场。
   同分按代码排序，目标每只占调仓前净资产的1/N；不足N只的名额留现金。
   同一开盘时段先卖后买；费用导致资金不足时按比例缩减买入。成交价与市场休市可使实际权重偏离目标。
   无有效开盘或成交量时等待至可交易日；下一轮调仓替换未完成订单。已完成市场不在非调仓日重复调仓。
4. 碎股份额模拟；不约束整手、涨跌停、流动性、交易限额。单边综合成本默认10基点，含手续费与滑点假设，
   不等同于各市场实际税费。未提供期末平仓，期末资产按收盘估值。
   人民币记账，美股及港股分别用USD/CNY和USD/CNY÷USD/HKD换算；成交用前日汇率，估值用当日已知收盘汇率。
   持仓价格超过10天未更新仍按最后价格估值并警示；缺少必要持仓价格或汇率时停止，不能当作零收益。
5. 年化收益按所选起止自然天数；波动与夏普按252交易日年化，无风险利率为0。
   短历史和少量成交仅供观察，不能据此认定长期有效；成本翻倍是压力测试，不是收益基准。
6. GitHub Token只发给GitHub，不发给行情代理、不写入导出文件。历史股票池和行情缓存于浏览器IndexedDB。
   数据文件包含私人预测，导出后应自行保管。清缓存仅清回测缓存，不影响首页股票数据或设置。

回测数据JSON格式（version: 1；可从页面导出后查看具体字段）
start/end：可回测日期范围；market：ALL/CNY/HKD/USD。
snapshots：按at提交时间恢复的股票数组，含sha、stocks（code/name/PE/PS预测参数/forecastUpdatedAt）。
assets：code/name/currency/anchorCap（货币单位，非亿元）/anchorClose/bars。
bars：date、rawClose、close（复权）、open（同复权口径）、volume；可选marketCap为该日历史真实市值，优先于固定股本估算。
fx：USD/HKD数组，每行date、close（每单位外币折人民币）。
source、warnings、failures、universeCount：数据来源与覆盖。
页面导入用于重跑数据，不把导入数据写回GitHub。

回测检查
node --test tests/backtest.cjs tests/score.cjs
node tests/backtest-ui.cjs
node tests/watchlists.cjs
后两项需要Chrome和playwright，可通过PLAYWRIGHT_MODULE指定模块路径。
