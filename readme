股票估值系统

自选组合
1. 在股票表格中勾选股票，点击「保存选中」，输入组合名称。
2. 下次在顶部「⭐ 自选」下拉框选择组合，即可查看全部成员。
3. 同一只股票可以加入多个组合；原有属性/赛道保持独立。
4. 选择组合后点击「管理」，可改名、追加选中、移除选中或删除组合。
   追加其他股票：先选「全部股票」并勾选，再切回目标组合，点击「管理 → 追加选中」。
   勾选会跨筛选保留；可点「清空」重新选择。
5. 组合自动保存在本机。点击「保存到 GitHub」后，其他设备拉取数据即可使用。
   页面刷新时会保留未上传的本地修改；手动拉取并确认覆盖会使用云端版本。
6. 切换组合会清除代码、属性、市场、列筛选和「仅选中」条件；保留排序和历史截面。
7. 删除组合或移除成员不会删除股票。移除最后一个成员时，空组合会自动消失。

数据兼容
data.json 仍然是股票数组；组合成员记录在每只股票可选的 watchlists 字符串数组中。
旧数据无需迁移，组合随现有 GitHub 保存/拉取流程同步。

Score
涨跌后为 Score 列，点击表头切换高低排序，点击得分查看明细。缺失评分排在最后。
总分 = 估值分×置信系数×60% + 趋势分×40%，保留一位小数。
每种估值方法分别计算：估值分 = 上行分×60% + 赔率分×40%。
上行分：上涨空间100%为100分，负空间计0，最高100分。
赔率分：赔率 = 上涨空间 / 下跌空间绝对值，4倍为100分，最高100分。
无上行空间时该方法计0分；下限目标不低于现价且有上行空间时，赔率分计100。
PE、PS均有效时取平均，仅一种有效时估值分打八折。
市值、预测或估值倍数必须是有效正数，估值下限不得超过上限。
趋势：MA5 > MA10 > MA20为100分，反向为0分，其他有效排列为50分。
无有效估值、市值或趋势数据不足时，总分显示「—」。
置信系数按自然月：≤2个月1，＞2至4个月0.7，＞4至6个月0.5，＞6个月0；日期缺失或异常取0。
置信系数仅调整估值分，趋势分不随预测日期衰减。
预测日期优先使用 forecastUpdatedAt；旧记录从净利、营收、首次创建或确认预测日志中读取。
修改净利或营收预测、新增股票或勾选「确认预测」后保存，会更新预测日期。
仅修改估值倍数、名称、标签、普通备注或自选组合不会刷新预测日期。
原有日期列仍显示最新操作日志日期；评分使用的预测日期可在得分明细中查看。
历史截面采用截面价格/趋势与现有预测，置信系数按今天计算，不是历史预测回测。
Score 动态计算，不写入 data.json；forecastUpdatedAt 随股票数据同步。

浏览器回归检查（使用模拟数据和 API，不访问真实股票数据）
安装 Chrome 和 playwright 后运行：node tests/watchlists.cjs
可通过 PLAYWRIGHT_MODULE 指定已有 playwright 的模块路径。
纯评分公式检查：node --test tests/score.cjs
